# Personal Site

## Setup
```
flutter clean
flutter pub get
```

## Running
```
flutter run -d chrome --release
```

## Deploying
```
flutter build web
cd build/web
git push --force --set-upstream origin main
```
